# Ensure 'create_dt' column is in datetime format and timezone-aware
df['create_dt'] = pd.to_datetime(df['create_dt'], utc=True)

# Apply the filter between start_date and end_date
filtered_df = df[(df['create_dt'] >= start_date) & (df['create_dt'] < end_date)]
