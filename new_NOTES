from datetime import datetime
import pytz

# Define EST timezone
est = pytz.timezone("America/New_York")

# Create datetime for full day in EST
start_date_est = est.localize(datetime(2025, 5, 29, 0, 0, 0))
end_date_est = est.localize(datetime(2025, 5, 29, 23, 59, 59, 999999))
