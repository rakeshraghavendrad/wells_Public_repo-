# Filter rows where all three columns have the value 1.0
df_filtered = df_1[(df_1['column_1'] == 1.0) & (df_1['column_2'] == 1.0) & (df_1['column_3'] == 1.0)]

# Create a new column 'summary' with the required message
df_filtered['summary'] = "All similarity scores are equal & No significant mismatch detected"
