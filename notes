df2.columns = df2.columns.str.replace("_space", "", regex=False)


# Merge with indicator to track differences
df_diff = df1.merge(df2, on=['ID', 'Name'], how='outer', indicator=True)

# Filter out non-matching rows
df_non_matching = df_diff[df_diff['_merge'] != 'both'].drop(columns=['_merge'])
