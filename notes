# Count occurrences of 'Profile_matched', 'Profile_notmatched', and Null (None)
match_counts = df['profile_match'].value_counts(dropna=False)
print("Counts of each match status:\n", match_counts)

# Calculate percentages of each category
match_percentages = (match_counts / len(df)) * 100
print("\nPercentage of each match status:\n", match_percentages)
