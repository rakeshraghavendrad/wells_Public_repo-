Each month, we receive new data, which we use to retrain three models: XGBoost (XGB), Generalized Linear Model (GLM), and Random Forest (RF). 
After training, we evaluate the performance metrics of each model and select the one with the best results to diagnose the issue.
