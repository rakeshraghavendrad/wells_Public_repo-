Below is the function of min_max_scaler for spark dataframe, which of the following statement is correct:

UE-1.png

Answer choices
Select only one option
The code will return the spark dataframe with two new columns (min and max).

The code will return error because the input and output use the same object name “df”

The code will return spark dataframe with new column names and the shape the dataframe is same as the input

The code will return error because it can not return dataframe
