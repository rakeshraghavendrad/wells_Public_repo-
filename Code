import org.apache.spark.sql.functions._

// Assuming your DataFrame is named "yourDataFrame" and the column is named "c3"
val resultDataFrame = yourDataFrame
  .withColumn("newColumn", split(col("c3"), ",")(1))
  .withColumn("newColumn", regexp_replace(col("newColumn"), "[\\(\\)]", ""))

resultDataFrame.show()
