SELECT
  Custnum,
  MIN(Ip_addar) AS first_login_ip,
  MAX(Ip_addar) AS second_login_ip,
  MAX(Evnt_start_dttm) - MIN(Evnt_start_dttm) AS time_difference_sec
FROM multloginevnt
GROUP BY Custnum
HAVING COUNT(DISTINCT Evnt_start_dttm) >= 2;
